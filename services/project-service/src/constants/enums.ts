export enum Platform {
  android = 'android',
  ios = 'ios',
  website = 'website'
}

export enum ProjectType {
  app = 'app',
  web = 'web'
}

export enum Status {
  Pending = 'Pending',
  Completed = 'Completed'
}
